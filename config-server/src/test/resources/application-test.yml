spring:
  cloud:
    config:
      server:
        git:
          uri: file://${java.io.tmpdir}/test-config-repo
          clone-on-start: false
          force-pull: false
  security:
    user:
      name: test-config-user
      password: test-config-pass

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false
